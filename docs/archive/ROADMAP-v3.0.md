# BBS Flash v3.0 — ROADMAP

**Версия:** 1.0
**Дата:** 2 августа 2026
**Источник истины по фактам:** `docs/bafang_intelligence.md` (Блок 1, Opus)
**Статус:** скоуп зафиксирован, готово к Блоку 2 (Sonnet)

---

## ⚠️ 0. ПОПРАВКИ К MASTER_GUIDE v1.0 — ПРИМЕНИТЬ ПЕРЕД РАЗРАБОТКОЙ

> Мастер-гайд загружается в начале **каждой** сессии. Пока эти ошибки в нём — каждый агент будет воспроизводить их заново. Это главный риск мульти-агентной схемы: отравленный источник истины.

| # | Где в гайде | Что написано | Как должно быть | Почему |
|---|---|---|---|---|
| **1** | Часть 8.2, таблица моторов | `BBS01 / BBS02 / BBSHD — Max RPM 130 ±5` (одинаково) | **BBS01 ~78–100 · BBS02 ~120 · BBSHD 130–150** | Ни один источник не подтверждает «130 для всех». Точность «±5» — признак выдумки. От max RPM зависит **вся** цифра Reality Check |
| **2** | Часть 8.2 | `BBS01 — 36V (31-42V)` | BBS01B существует и в **48V** | Фактическая ошибка |
| **3** | Часть 8.3, код 21 | `21 — Overcurrent` | **21 — Датчик скорости (Speed Sensor Abnormal)** | Перепутано. Частая причина — сместившийся магнит на спице |
| **4** | Часть 8.3, код 08 | `08 — Throttle fault` | **02 — газ; 08 — контроллер/мотор** | Перепутаны коды |
| **5** | Часть 1.3, боль №3 | «Отсутствие телеметрии: невозможно видеть ток, **температуру**» | **Датчика температуры в BBS02 нет.** Переформулировать боль | Требование физически невыполнимо (см. §1 ниже) |
| **6** | Части 3C, 4, 7 | Тёмная тема `#0a0e1a`, неон, обязательно | **Светлая rally по умолчанию + тёмная по переключателю** | Прямой ответ заказчика в /grill-me. Часть 6B гайда сама предусматривает toggle |
| **7** | Часть 4, модуль 7 + Часть 7.2 | `AI Tuning (TensorFlow.js)` на «данных сообщества» | **Вычеркнуть из v3.0** | Датасета не существует. Обучать не на чем |
| **8** | Часть 7.4 | `AR Preview (WebXR)` | **Вычеркнуть из v3.0** | Максимальная стоимость, минимальная польза |
| **9** | Часть 9.1 DoD | «UI не лагает при телеметрии 10 Гц» | Заменить на критерий стендового режима | Дорожная телеметрия недостижима (§1) |
| **10** | Часть 9.2 | «Тест Reality Check (32T → 44 км/ч)» | **44–48 км/ч** (вилка) | 44 = нижняя граница; при 120 об/мин выходит 47.8 |
| **11** | Часть 0.3, п.6 | `Service Worker + IndexedDB` | Оставить IndexedDB, **явно запретить localStorage** | localStorage не работает в среде артефактов |
| **12** | Приложение A | «GitHub repo `bafang-uart-protocol`» | Такого репозитория нет. Реальные: **Penov (Delphi source)**, `bafang-python` (Sandhaus), BafangWebConfig, OpenBafangTool | Ссылка на несуществующий источник |

---

## 1. ЧТО ФИЗИЧЕСКИ ВОЗМОЖНО (инвентарь железа)

Ключевой раздел. Всё, что вне этой таблицы, — не проектируем.

| Данные | Доступ | Решение для v3.0 |
|---|---|---|
| Все параметры конфигурации (Basic/PAS/Throttle) | UART 1200, протокол разобран, **работает в v2.0** | ✅ **Ядро продукта** |
| General block (производитель, модель, HW/FW, ном. V, max A) | UART 1200, работает | ✅ Автоопределение модели |
| Скорость, мощность (Вт), напряжение, заряд %, PAS, коды ошибок | Протокол **дисплея** (не конфига) | 🟡 Только «стендовый режим» |
| ODO / Trip / Cap-out мАч | Читается с экрана 850C глазами | 🟢 Ручной ввод в аналитику |
| **Температура мотора** | **Не существует** | ❌ Не рисовать вообще |
| **Каденс** | Контроллер меряет, но наружу не отдаёт | ❌ Только **расчётный** в симуляторе |

### Почему нет дорожной телеметрии — два блокера, не обходимых софтом

1. **Разъём один.** Кабель конфигуратора включается туда же, где дисплей. Либо ноутбук, либо дисплей.
2. **Протокол конфигурации ≠ протокол дисплея.** Живые данные требуют эмуляции 850C — отдельная реализация, не расширение.

**Вывод:** боль №1 (перегрев на апхилах) решается **не телеметрией, а Reality Check + обучением каденсу**. Это единственный работающий путь без нового железа.

---

## 2. ПРИОРИТЕТЫ

### P0 — без этого продукта нет

| Модуль | Что делает | Почему P0 |
|---|---|---|
| `BafangProtocol` | Read/Write UART, checksum, валидация ответов | Уже работает в v2.0, переносится |
| `AutoDetect` | BBS01/02/HD из General block → подстройка лимитов | Дешёво, предотвращает запись 25A в BBS01 |
| **`RealityCheck`** | Предел по передаче + по мощности + тепловая эвристика | **Главная ценность.** То, чего нет в оригинале Пенова |
| `SafetyGate` | Авто-бэкап .el перед записью, confirm, preview дельты | Правило «сохрани копию ПЕРЕД» — золотое во всех гайдах |

### P1 — то, ради чего это красивое

| Модуль | Что делает |
|---|---|
| `PhysicsSimulator` | Расчёт скорости/каденса; **Crr/CdA переключаемые**, не константы |
| `RallyCluster` | Стрелочный спидометр (красная зона = `v_gear_limit`), шкала тока, тахометр каденса с «холодной зоной» 70–100 |
| `ComparativeBars` | A/B дельты. **Инверсия цвета** для пикового тока и тепловой нагрузки |
| `BikeAnimation` | Наклон = уклон, колёса = скорость, педали = каденс + цвет зоны. Трассы: город / лес |
| `Tooltips` | Все 20+ параметров: что делает / ↑ / ↓ / ⚠️ |

### P2 — полировка

`ThemeToggle` (rally light ↔ night neon на одних CSS-переменных) · `BenchMode` (телеметрия в гараже) · PWA + IndexedDB · `WireLog` с фильтрами · Keyboard shortcuts

### P3 — только если появится железо/данные

Внешний термистор + ESP32-логгер · WebGPU 3D-график · Web Bluetooth · Voice Commands

### ❌ Вне скоупа v3.0

AI Tuning на данных сообщества · WebXR/AR · Telemetry Cloud Sync · Дорожная live-телеметрия 10 Гц

---

## 3. СПРИНТЫ

| Спринт | Содержание | Выход | Готово, когда |
|---|---|---|---|
| **S1** | Перенос протокола из v2.0 + AutoDetect + SafetyGate | `BafangProtocol.js` | Read All + авто-бэкап + Write All с confirm работают на живом SZZ9 |
| **S2** | RealityCheck + PhysicsSimulator | `RealityCheck.js` | 32T/11T/27.5×2.8 → выдаёт 44–48 км/ч и объясняет, почему |
| **S3** | Rally-приборка + анимация байка | UI | Стрелка спидометра, красная зона от передач, тахометр каденса с зонами |
| **S4** | ComparativeBars + тултипы + локализация | UI | Все параметры с ↑/↓/⚠️ на русском |
| **S5** | QA (Блок 3) → фиксы → PWA → деплой | `index.html` | Чеклист §5 пройден |

**Правило из мастер-гайда:** максимум 3 итерации на модуль, дальше — эскалация к Антону.

---

## 4. P0-БЛОКЕРЫ ВЕРИФИКАЦИИ

Без этих двух замеров Reality Check будет врать. Проверяются на стенде: заднее колесо вывешено.

| # | Что | Как | Блокирует |
|---|---|---|---|
| **V1** | Max RPM контроллера HZXT SZZ9 FW V2.0.1.1 | Полный газ на высшей передаче, снять каденс с дисплея | S2 целиком |
| **V2** | Минимально принимаемое Time of Stop | Записать 10 → прочитать. Отвергнуто → 15 → 20 | Совет по защите цепи |

Остальные 6 пунктов — в §8 `bafang_intelligence.md`, не блокирующие.

---

## 5. DEFINITION OF DONE (исправленный)

- [ ] Read/Write всех трёх блоков на живом железе, **.el байт-в-байт совместим с Penov tool**
- [ ] Авто-бэкап .el **до** любой записи; Write All заблокирован без бэкапа
- [ ] Reality Check выдаёт **44–48 км/ч** для 32T/11T/27.5×2.8 и объясняет причину
- [ ] AutoDetect не даёт записать >18A в BBS01 и >25A в BBS02
- [ ] Тепловое предупреждение при `ток ≥22A AND расчётный каденс <60 об/мин`
- [ ] Тултипы у всех параметров: что делает / ↑ / ↓ / ⚠️
- [ ] ComparativeBars: рост пикового тока и тепловой нагрузки = **красный**, не зелёный
- [ ] Нигде в UI нет полей «температура мотора» и «каденс» как telemetry
- [ ] Web Serial отсутствует → UI работает, понятное сообщение, симулятор доступен
- [ ] Обе темы на одном наборе CSS-переменных, светлая по умолчанию
- [ ] `prefers-reduced-motion` отключает анимацию байка
- [ ] Ноль `localStorage` в коде (только IndexedDB)
- [ ] Дисклеймер: приложение меняет **настройки**, не прошивку

---

## 6. ДЕПЛОЙ

**Критично: Web Serial требует HTTPS.** По HTTP подключение к мотору не заработает — то есть красивая веб-версия потеряет главную функцию. Сертификат обязателен (Let's Encrypt / Caddy автоматом).

Инструкции — в `docs/DEPLOY.md`.

---

## 7. ОТКРЫТЫЕ ВОПРОСЫ

1. **Барьер для чужих пользователей.** Решено: аудитория — только владельцы BBS02. Но незнакомец с BBS01 36V может записать профиль на 24A. AutoDetect закрывает это технически, но нужен ли ещё и явный экран-предупреждение при первом входе?
2. **Калибровка физмодели.** До замеров V1/V2 цифры симулятора — оценка. После пары заездов (разгон 0–30 на известном PAS) модель можно подогнать под конкретный мотор. Делаем в v3.1?
3. **Публикация в сообществе.** Endless Sphere / Reddit — только после S5 и проверки на железе.
